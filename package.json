{
  "name": "vtr-api-server",
  "private": true,
  "version": "1.0.0",
  "description": "",
  "main": "server.js",
  "engines": {
    "node": "18.12.1"
  },
  "scripts": {
    "start": "node server.js",
    "clean": "rm -rf node_modules && npm i && cd client && rm -rf node_modules && rm -rf dist && npm i && npm run dev"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.15.4"
  }
}
